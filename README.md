for Windows
C:\ProgramData\Ableton\Live 12 Standard\Resources\MIDI Remote Scripts

Create a folder `AbletonMCP`, then put the `__init__.py` under it.

go to ableton live settings -> Link, Temp & MIDI -> Control Source. select `AbletonMCP` from the list.
